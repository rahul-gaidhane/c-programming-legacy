#include<stdio.h>
#define string "does string\nconstants could contain\nnewlinw\ncharacter"
void main()
{
/*	int x = 10, *px;
	float y = 20.00, *py;
	char z = 'c', *pz;
	px=&x;
	py=&y;
	pz=&z;
	printf("&x = %p\n&y = %p\n&z = %p\n",&x,&y,&z);	
	printf("px = %p\npy = %p\npz = %p\n",px,py,pz);
	printf("*px = %d\n*py = %f\n*pz = %c\n",*px,*py,*pz);
	printf("&px = %p\n&py = %p\n&pz = %p",&px,&py,&pz);
	px++;
	py++;
 	pz++;	
	printf("px = %p\npy = %p\npz = %p\n",px,py,pz);
	px++;
	printf("px = %p\npy = %p\npz = %p\n",px,py,pz);
*/
/*	int *pa, i;
	char *pc;
	float *pb;
	int a[] = {1,2,3,4,5};
	char c[] = {'a','b','c','d','e'};
	float b[] = {3.5,2.6,6.5,5.5,8.6};
	for (i = 0;i < 5; i++)
		printf("a[%d] = %p\n",i,&a[i]);	
	for (i = 0; i < 5; i++)
		printf("c[%d] = %p\n",i,&c[i]);
	for (i = 0; i < 5; i++)
		printf("b[%d] = %p\n",i,&b[i]);
	pa = a;
	pc = c;
	pb = b;
	for (i = 0;i < 5; i++)
		printf("a[%d] = %d\n",i,*pa++);	
	for (i = 0; i < 5; i++)
		printf("c[%d] = %c\n",i,*pc++);
	for (i = 0; i < 5; i++)
		printf("b[%d] = %f\n",i,*pb++);*/
	printf(string);

	
}
